<UserControl x:Class="UI.Views.NavBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="auto" Height="auto">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Height="50" Width="120" Grid.Row="0" Margin="0,10,0,0">录入</Button>
        <Button Height="50" Width="120" Grid.Row="1" Margin="0,10,0,0">查询</Button>
        <Button Height="50" Width="120" Grid.Row="2" Margin="0,10,0,0">设置</Button>
    </Grid>
</UserControl>
